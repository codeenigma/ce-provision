nodes:
  # Indexer server nodes
  indexer:
    - name: node-1
      ip: 172.18.0.36

  # Wazuh server nodes
  # Use node_type only with more than one Wazuh manager
  server:

  # Dashboard node
  dashboard:
