---
dependencies:
  - role: _meta/common_base
  - role: firewall_config
  - { role: ssh_server, when: not is_local }
  - role: user_provision
  - role: user_deploy
  - role: mysql_client
  - role: nodejs
  - role: php-cli
  - role: php-fpm
  - role: opcache
  - role: nginx
