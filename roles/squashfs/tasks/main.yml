- name: Ensure squashfs FUSE support is installed.
  ansible.builtin.apt:
    name: squashfuse
    state: present
    update_cache: true

- name: Ensure squashfs tools are installed.
  ansible.builtin.apt:
    name: squashfs-tools
    state: present
    update_cache: true
