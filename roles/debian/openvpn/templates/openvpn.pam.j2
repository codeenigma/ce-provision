# {{ ansible_managed }}
# Basic config taken from
# https://wiki.ipfire.org/configuration/services/openvpn/extensions/auth/auth-pam

auth    required        pam_unix.so    shadow    nodelay
account required        pam_unix.so
